name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate repository scaffold
        run: |
          test -f README.md
          test -f PLAN.md
          test -f LICENSE
          test -f plugins/example-provider/plugin.json
          test -f registry/providers.json

